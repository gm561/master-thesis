# partly autogenerated, that's why it's so ugly!

# say 'make V=1' to see the full build output
ifndef V
Q = @
Q_LATEX = sh -c 'echo -e "   " `echo $$(basename "$$0") | tr a-z A-Z`"\t$${!\#/..\/..\//}"; ! "$$0" "$$@" | grep -A5 -E "^[^ ]+:[0-9]+:"'
Q_OTHER = sh -c 'echo -e "   " `echo $$(basename "$$0") | tr a-z A-Z`"\t$${!\#/..\/..\//}"; "$$0" "$$@" >/dev/null'
endif

LATEX = latex
PDFLATEX = pdflatex
BIBTEX = bibtex
DVIPS = dvips
DVIPDF = dvipdf
PDFTOEPS = pdftops -eps
PDFTOPS = pdf2ps
EPSTOPDF = epstopdf
FIGTODEV = fig2dev
MPOST = mpost

.PHONY: all clean archive

all:  presentation

texfiles = $(wildcard *.tex)

.PHONY: presentation
presentation: presentation.pdf

presentation.pdf: $(texfiles)
	$(Q) rm -rf build/pdf/; mkdir -p build/pdf/
	$(Q) cd build/pdf/ && TEXINPUTS="../../:$$TEXINPUTS" $(Q_LATEX) $(PDFLATEX) -interaction=nonstopmode -file-line-error ../../presentation.tex
	$(Q) cd build/pdf/ && TEXINPUTS="../../:$$TEXINPUTS" $(Q_LATEX) $(PDFLATEX) -interaction=nonstopmode -file-line-error ../../presentation.tex
	$(Q) cd build/pdf/ && TEXINPUTS="../../:$$TEXINPUTS" $(Q_LATEX) $(PDFLATEX) -interaction=nonstopmode -file-line-error ../../presentation.tex
	$(Q) ln -f build/pdf/presentation.pdf presentation.pdf

presentation.aux: $(texfiles)
	$(Q) $(Q_LATEX) $(LATEX) -interaction=nonstopmode -file-line-error presentation.tex

clean:
	rm -rf build/
	rm -f *.aux
	rm -f presentation.dvi presentation.idx presentation.ind presentation.ilg presentation.out presentation.toc presentation.pdf presentation.log

# archive: presentation-template.zip

# presentation-template.zip: *.tex Makefile ETHlogo.* refs.bib
# 	mkdir -p presentation
# 	cp `git ls-files "*.tex" Makefile "ETHlogo.*" refs.bib` presentation/
# 	perl -i -pe 's/\$$VERSION\$$/'"$$(git describe)/" presentation/*.tex
# 	rm -f presentation-template.zip
# 	zip -r presentation-template.zip presentation/
